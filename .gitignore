# Go
/bin/
*.test
*.out

# Env / secrets
.env
*.local

# Node
web/node_modules/
web/dist/

# Build artifacts / scratch
/tmp/

# OS
.DS_Store
Thumbs.db
*.exe

# Session tooling — ignore .claude/ scratch (worktrees, local settings), but
# keep the checked-in agent/command config under version control.
.playwright-mcp/
.claude/*
!.claude/agents/
!.claude/commands/
.superpowers/
/docs/superpowers/
# Local-only brand tooling: generates favicon.ico / logo candidates. The
# OUTPUT is committed, the generator is not.
scripts/build-favicon-ico.mjs
scripts/generate-logo.mjs
