apiVersion: v1
kind: Service
metadata:
  name: {{ include "inroad.fullname" . }}-api
  labels:
    {{- include "inroad.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inroad.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
