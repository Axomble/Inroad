// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: results.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const campaignOutcomeResults = `-- name: CampaignOutcomeResults :many
WITH last_send AS (
    SELECT DISTINCT ON (contact_id) contact_id, step_order, variant_id
    FROM sends
    WHERE workspace_id = $2 AND campaign_id = $1 AND status = 'sent'
    ORDER BY contact_id, step_order DESC, sent_at DESC
)
SELECT ls.step_order, ls.variant_id, e.stop_reason::text AS stop_reason, count(*)::bigint AS n
FROM sequence_enrollments e
JOIN last_send ls ON ls.contact_id = e.contact_id
WHERE e.workspace_id = $2 AND e.campaign_id = $1 AND e.stop_reason IS NOT NULL
GROUP BY ls.step_order, ls.variant_id, e.stop_reason
ORDER BY ls.step_order, ls.variant_id NULLS FIRST
`

type CampaignOutcomeResultsParams struct {
	CampaignID  uuid.UUID `json:"campaign_id"`
	WorkspaceID uuid.UUID `json:"workspace_id"`
}

type CampaignOutcomeResultsRow struct {
	StepOrder  int32       `json:"step_order"`
	VariantID  pgtype.UUID `json:"variant_id"`
	StopReason string      `json:"stop_reason"`
	N          int64       `json:"n"`
}

// Stopped enrollments grouped by the step and variant of the LAST message that
// contact received, plus the stop reason.
//
// Attributing an outcome to the last send is a judgement, and it is the right
// one: a reply answers the message the person just read, and a bounce is
// produced by the delivery attempt that just happened. The alternative --
// spreading an outcome across every step that contributed -- would let a step
// that merely preceded the reply take credit for it, and would make the
// per-variant comparison unusable, since both arms of a later step would inherit
// the same earlier sends.
//
// The consequence worth knowing: on a multi-step campaign, replies concentrate
// on whichever step actually landed them, so a step's reply count is NOT
// "replies from people who received this step".
//
// DISTINCT ON picks that last message per contact. Ordered by step_order DESC
// then sent_at DESC so a re-sent step (a reclaimed stale lease) cannot make an
// earlier step look like the most recent one.
func (q *Queries) CampaignOutcomeResults(ctx context.Context, arg CampaignOutcomeResultsParams) ([]CampaignOutcomeResultsRow, error) {
	rows, err := q.db.Query(ctx, campaignOutcomeResults, arg.CampaignID, arg.WorkspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CampaignOutcomeResultsRow
	for rows.Next() {
		var i CampaignOutcomeResultsRow
		if err := rows.Scan(
			&i.StepOrder,
			&i.VariantID,
			&i.StopReason,
			&i.N,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const campaignSendResults = `-- name: CampaignSendResults :many

SELECT s.step_order,
       s.variant_id,
       count(*)::bigint AS sent,
       count(*) FILTER (WHERE o.send_id IS NOT NULL)::bigint AS opens,
       count(*) FILTER (WHERE c.send_id IS NOT NULL)::bigint AS clicks
FROM sends s
LEFT JOIN (
    SELECT DISTINCT te.send_id
    FROM tracking_events te
    JOIN sends js ON js.id = te.send_id AND js.workspace_id = te.workspace_id
    WHERE te.workspace_id = $2 AND te.campaign_id = $1 AND te.kind = 'open'
      AND te.user_agent NOT ILIKE '%GoogleImageProxy%'
      AND (js.sent_at IS NULL OR te.created_at > js.sent_at + interval '2 seconds')
) o ON o.send_id = s.id
LEFT JOIN (
    SELECT DISTINCT send_id
    FROM tracking_events
    WHERE workspace_id = $2 AND campaign_id = $1 AND kind = 'click'
) c ON c.send_id = s.id
WHERE s.workspace_id = $2 AND s.campaign_id = $1 AND s.status = 'sent'
GROUP BY s.step_order, s.variant_id
ORDER BY s.step_order, s.variant_id NULLS FIRST
`

type CampaignSendResultsParams struct {
	CampaignID  uuid.UUID `json:"campaign_id"`
	WorkspaceID uuid.UUID `json:"workspace_id"`
}

type CampaignSendResultsRow struct {
	StepOrder int32       `json:"step_order"`
	VariantID pgtype.UUID `json:"variant_id"`
	Sent      int64       `json:"sent"`
	Opens     int64       `json:"opens"`
	Clicks    int64       `json:"clicks"`
}

// Per-step and per-variant campaign results.
//
// Two queries rather than one, because they have DIFFERENT GRAINS. Engagement
// (opens, clicks) is per SEND: a multi-step campaign sends several times per
// contact and each message is tracked separately. Outcomes (replied, bounced,
// unsubscribed) are per ENROLLMENT: an enrollment stops at most once for the
// whole sequence. Joining them into one statement would make one of the two
// counts wrong by a factor of the step count, which is exactly the mistake the
// campaign rollup's two denominators already exist to avoid.
// Sends, indicative opens and clicks, grouped by step and variant.
//
// Every numerator is defined identically to the campaign-wide rollup
// (CountSendsByStatus, CountHumanOpens, CountEngagedSendsByKind), so a variant
// breakdown always sums to the campaign total rather than telling a second
// story. In particular the open filter repeats the proxy exclusion verbatim: a
// variant comparison built on raw pixel hits would rank whichever arm reached
// more Gmail users.
//
// variant_id is NULL for the step's own base copy (migration 000053), which is
// also every send made before variants existed -- so historic campaigns report
// as a single unnamed arm rather than as missing data.
func (q *Queries) CampaignSendResults(ctx context.Context, arg CampaignSendResultsParams) ([]CampaignSendResultsRow, error) {
	rows, err := q.db.Query(ctx, campaignSendResults, arg.CampaignID, arg.WorkspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CampaignSendResultsRow
	for rows.Next() {
		var i CampaignSendResultsRow
		if err := rows.Scan(
			&i.StepOrder,
			&i.VariantID,
			&i.Sent,
			&i.Opens,
			&i.Clicks,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
