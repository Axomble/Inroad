// Generated RTK Query hooks for the campaigns domain (plus the mailbox + list
// queries the create form's pickers need), re-exported so feature components
// import from their own folder. Never hand-edit store/api.ts.
export {
  useListCampaignsQuery,
  useGetCampaignQuery,
  useCreateCampaignMutation,
  useLaunchCampaignMutation,
  useListMailboxesQuery,
  useListListsQuery,
} from '@/store/api'
